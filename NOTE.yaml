as of 9th FEB, 2025
- convert file considers the last item as the final item in the grouped jv, which also helps in general grouping. but the total counts and figures gets corrupted.
- convertv2 file ungroups everything so the counts and figures are correct, but the pages increases.
- convertV1 is the original
- updated the convert code.. and these are the below changes done 
Done! I've updated the code to properly accumulate and display multiple figures when JV numbers with the same name/bill description and allocation code appear together:

Key changes:

Grouping logic: Modified to accumulate values when the same CO6 and allocation appears multiple times, instead of overwriting
Display logic: Updated to show all values in each allocation column with proper line breaks between them, and calculate subtotals correctly for all values
Now if you have multiple JVs with the same CO6/bill description, they'll be grouped into one row, with all their figures displayed for their respective allocation columns.